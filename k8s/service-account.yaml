apiVersion: v1
kind: ServiceAccount
metadata:
  name: eulard-sa
  namespace: eulard
  labels:
    app.kubernetes.io/part-of: eulard
  annotations:
    # Workload Identity: binds this K8s SA to the GCP SA
    iam.gke.io/gcp-service-account: eulard-sa@kelihi-ai-platform.iam.gserviceaccount.com
